import { makeStyles } from '@material-ui/core'

export default makeStyles(({ palette, typography }) => {
  return {
    root: {
      flexGrow: 1,
    },
    tableContainer: {
      width: '100%',
      marginTop: 30,
    },
  }
})
