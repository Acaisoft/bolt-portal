export default ({ palette, shape, spacing, typography }) => {
  return {
    content: {
      flexGrow: 1,
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    },
    actions: {
      justifyContent: 'center',
    },
  }
}
