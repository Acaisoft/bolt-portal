export default ({ spacing }) => ({
  root: {
    margin: spacing.unit * 2,
  },
  holder: {
    display: 'flex',
    flex: '1 1 auto',
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
})
