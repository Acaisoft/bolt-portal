import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  linkItem: {
    fontWeight: 'normal',
  },
  textItem: {
    fontWeight: 'bold',
  },
  separator: {
    color: '#F76F40',
  },
}))
