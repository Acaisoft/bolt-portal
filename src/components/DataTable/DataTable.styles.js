export default ({ palette }) => ({
  responsiveContainer: {
    overflowX: 'auto',
  },
  stripedOdd: {
    backgroundColor: palette.background.striped.odd,
  },
  stripedEven: {
    backgroundColor: palette.background.striped.even,
  },
})
